/** Back-compat re-export — canonical weights in PredictionEngine/weights. */
export {
  DEFAULT_PREDICTION_WEIGHTS,
  getPredictionWeights
} from "../PredictionEngine/weights.js";

export type { PredictionWeights } from "../PredictionEngine/types.js";
